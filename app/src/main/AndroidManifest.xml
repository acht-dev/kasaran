<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cerdastb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MateriVideoActivity"></activity>
        <activity android:name=".ListMateri" />
        <activity android:name=".Biodata" />
        <activity android:name=".Kalendar" />
        <activity android:name=".Alarm" />
        <activity android:name=".user_detil" />
        <activity android:name=".TampilUser" />
        <activity android:name=".Hadmin" />
        <activity android:name=".BukuSaku" />
        <activity android:name=".TglDia" />
        <activity android:name=".Pretest" />
        <activity
            android:name=".beranda"
            android:label="@string/title_activity_beranda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Input_Bio" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>